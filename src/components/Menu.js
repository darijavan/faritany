import React from "react";

const styles = {
  container: {
    height: '100%',
    widht: '100%',
    textAlign: 'center'
  }
};

export function Menu() {
  return (
    <div style={styles.container}>Menu</div>
  );
}